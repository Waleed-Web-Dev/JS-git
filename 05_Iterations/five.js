//for Each
let arr = [43,35,3535,33,5];

arr.forEach( (value, index, arr)=> {
    console.log(`Value at index ${index} is ${value} and the whole array is ${arr}`);
});

function con(kk) {
    console.log(kk);
}

// arr.forEach(con)

let arr2 = [
    {
        prog: "jaj",
        kdgjbs: "Af"
    },
    {
        prog: "DFg",
        kdgjbs: "FA"
    }
]


arr2.forEach((ind) => {
    // console.log(ind.prog)
})